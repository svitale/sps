define([
  'underscore',
  'backbone',
], function(_, Backbone) {    
    var SequenceModel = Backbone.Model.extend({
/*
        initialize: function () {
        },
        doChange: function(evt) {
        }
*/
    });
    return SequenceModel;
});
